apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: soat-products
  labels:
    app: product-service
spec:
  type: LoadBalancer
  selector:
    app: product-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080