apiVersion: v1
kind: Service
metadata:
  name: products-app
  namespace: products
spec:
  type: LoadBalancer
  selector:
    app: products-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080